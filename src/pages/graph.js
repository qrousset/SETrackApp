import React from 'react';

const Graph = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Graph</h1>
    </div>
  );
};

export default Graph;